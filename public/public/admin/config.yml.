backend:
  name: git-gateway
  branch: main  # ou 'master' selon le nom de ta branche principale

media_folder: "public/images/uploads"  # Où seront stockés les fichiers uploadés
public_folder: "/images/uploads"

collections:
  - name: "pages"  # Nom de la collection
    label: "Pages"
    folder: "src/pages"  # Dossier où se trouvent tes pages
    create: true
    slug: "{{slug}}"
    fields:
      - { label: "Title", name: "title", widget: "string" }
      - { label: "Body", name: "body", widget: "markdown" }
